Heavyweight correct fix over workaround. Shares credentials when stuck. Setup must survive reboots. Expects I already have API keys — check before asking. Terse communicator ("done", "proceed", "Restarted") — handles restarts himself, present the command.
§
Design: aurora palette, no glows. Telegram = UX north star. Dashboard: real tiles, chat sidebar flush full-height. Perf: Intel UHD 630 — avoid box-shadow/GLSL, prefer canvas.
§
Dashboard: real tiles with live data. Memory galaxy = Supabase/references/cold store. Chat sidebar: flush full-height, solid bg.
§
Coder fleet round-robin (coder→coder-d), claude-sonnet-4-6. MECHANISM: failure→checkable cause. 'Review the repo' = plain status only. Expects thorough verification — full audit over 'done'.
§
MECHANISM doctrine: failure → checkable cause, not apology. Worker fix fails → name what it missed BEFORE re-routing. Stubborn bugs after multiple delegations → own it inline.
§
'Review the repo' = plain status: branch, working tree, unpushed commits, live bundle vs dist. No proposals, no actions.
§
Prefers proactive/real-time systems over batch/session-end. Evaluates architecture holistically. Wants comprehensive solutions + proactive suggestions. Alerts → Telegram + dashboard.
§
End-to-end pipelines, not shortcuts. Webhooks→knowledge→Kanban→Telegram. Autonomous intake, no manual gates. SDK instrumentation, not just endpoints. Dedicated Telegram channels per integration.
§
Expects thorough verification before claiming something done. Push back with 'are we sure?' — prefers full audit over 'done and pushed'. End-to-end pipelines, not shortcuts (e.g. Sentry cards through Linear, not direct DB inserts).
§
Skill descriptions: first 60 chars are the only signal that survives truncation in the skills list. Put key terms (domain, pitfalls, verbs) in the first 60 — not trailing detail.
§
Prefers proactive repo hygiene: split feature work into feat/* branches on GitHub. When a logical fault line exists, offer branch candidates unprompted.