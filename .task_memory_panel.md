You are implementing a feature in the Hermes Dispatcher repo at /root/hermes-dispatcher.

GOAL: Replace the mock memory galaxy with REAL data from MEMORY.md/USER.md and add a MEMORY.md/USER.md editor. Two parts: a FastAPI backend router and frontend changes to Memory.tsx (+ a small data helper). Do NOT touch any other panel files.

KEY FACTS
- HERMES_HOME: /root/.hermes/
- Memory file: /root/.hermes/memories/MEMORY.md   (cap 2200 chars)
- User file:   /root/.hermes/memories/USER.md     (cap 1375 chars)
- References dir: /root/.hermes/references/  (may or may not exist; handle gracefully)
- Frontend dev server proxies /api -> http://localhost:8787 (see app/vite.config.ts). So the backend MUST serve on port 8787 and all endpoints live under /api/memory/...
- Python venv: /root/hermes-dispatcher/.venv (has fastapi + uvicorn). Use it.
- Galaxy renderer is app/src/components/memory/useGalaxy.ts. It consumes a GalaxyData object: { nodes: MemNode[], links: [number,number][], tiers: MemTier[] }. MemNode requires fields: id, tier, tierLabel, color, title, importance (0..1 number), ageDays (number), recall (number), x, y, z, label?. Do NOT change useGalaxy.ts's expected shape.

=== PART 1: BACKEND ===
Create /root/hermes-dispatcher/routes/memory.py exposing a FastAPI APIRouter named `router` with prefix "/api/memory":

1. GET /files -> {"memory": <MEMORY.md text>, "user": <USER.md text>, "memory_chars": N, "user_chars": N, "memory_cap": 2200, "user_cap": 1375}. If a file is missing, return empty string and 0 chars.

2. PUT /files -> body {"file": "memory"|"user", "content": "..."}. Write content to the matching file (MEMORY.md or USER.md under /root/.hermes/memories/). Create a timestamped .bak before overwriting if the file exists. Reject any file value other than "memory"/"user" with HTTP 400. Return {"ok": true, "chars": N}.

3. GET /galaxy -> {"nodes": [{"id","label","tier","body"}], "edges": []}
   - Parse MEMORY.md into entries: split on lines that are exactly "---" OR on the "§" delimiter (try both; whichever yields multiple entries). Fall back to splitting on blank lines / bullet lines if no delimiter present, so we always get reasonable entries. Each non-empty entry -> {id, label: first 40 chars of the entry (single-lined, stripped), tier: "hot", body: full entry text}.
   - Parse USER.md the same way with tier "warm".
   - Add up to ~6 tier "cold" nodes from filenames in /root/.hermes/references/ (just the filename as label and body; no file contents). If the dir is missing, skip.
   - ids must be unique strings (e.g. "mem-0", "usr-0", "ref-0").

Resolve HERMES_HOME from the HERMES_HOME env var, defaulting to /root/.hermes. Use pathlib.

ALSO: make routes/memory.py runnable standalone so it is verifiable. At the bottom add:
    app = FastAPI()
    app.add_middleware(CORSMiddleware, allow_origins=["*"], allow_methods=["*"], allow_headers=["*"])
    app.include_router(router)
    if __name__ == "__main__": uvicorn.run(app, host="0.0.0.0", port=8787)
Keep the APIRouter as the primary export so it can be mounted by a larger app later.

=== PART 2: FRONTEND ===
Edit app/src/components/panels/Memory.tsx and add a small helper (you may add a function in app/src/data/phase3.ts OR a new file app/src/data/memoryGalaxy.ts — your choice, but do not break existing exports).

Requirements:
A. Add a tab bar with two tabs: "Galaxy" and "Editor". Galaxy tab shows the existing canvas galaxy. Editor tab shows the editor described below. Keep the existing header.

B. Galaxy data must come from GET /api/memory/galaxy instead of the mock buildGalaxy(). Since useGalaxy needs full MemNode geometry (x,y,z, importance, recall, ageDays, tierLabel, color), write a transform that maps the API nodes (id,label,tier,body) into GalaxyData:
   - tier mapping: hot -> color #f6b73c label "Memory", warm -> color #5aa2f0 label "User Profile", cold -> color #6a7088 label "References".
   - Use a deterministic layout similar to existing buildGalaxy(): cluster centers per tier, seeded pseudo-random scatter for x/y/z, compute nearest-3 links, set importance (e.g. 0.5..0.95), recall, ageDays with deterministic values, and label the most-important node per tier. title = label. Reuse/adapt the math already in buildGalaxy() in phase3.ts so the visual stays good. The node info card detail can use the body text (galaxyDecor currently builds detail from importance band — keep it working; you may pass body through if convenient but it's optional).
   - Fetch in a useEffect with useState; show the galaxy once data loads. Handle the loading/empty state without crashing (galaxy with 0 nodes must not throw).

C. Editor tab: fetch GET /api/memory/files on mount. Two side-by-side panels (flex row, stack on narrow widths is fine), labeled "Memory" (MEMORY.md) and "User Profile" (USER.md). Each panel: a full-height <textarea> bound to state, a live char count and "% of cap" using memory_cap/user_cap from the API response (turn red/amber when over ~90%/100%), and a "Save" button that PUTs {file, content} to /api/memory/files and shows a brief saved/error status. After save, refresh char counts from the response.

D. Keep the spacebar-pause behavior already present; ensure it still ignores keystrokes while typing in the textarea (it already checks INPUT/TEXTAREA/SELECT).

CONSTRAINTS
- Do NOT modify any panel file other than Memory.tsx. Helper data files (phase3.ts or a new memoryGalaxy.ts) and the new routes/memory.py are fine.
- TypeScript must compile. Run `npx tsc -b` in app/ (or `npm run build`) and fix any type errors you introduce.
- Style: match the existing dark dashboard aesthetic (inline styles like the rest of Memory.tsx).

VERIFY before finishing:
1. cd /root/hermes-dispatcher && .venv/bin/python -c "import routes.memory" (ensure it imports; add routes/__init__.py if needed for the import, but the router file itself should also import standalone).
2. Start backend: .venv/bin/python routes/memory.py & then curl localhost:8787/api/memory/files and /api/memory/galaxy and a PUT round-trip, then kill it. Confirm JSON shapes match the spec.
3. cd app && npx tsc -b  (must pass with no new errors).
Report exactly what you changed and the verification command outputs.
