# Evaluated Tools Log

Running log of third-party repos/skills/tools reviewed and the verdict. Check here
before re-reviewing something — avoids re-litigating a prior decision.

| Date | Repo / Tool | Verdict | Why | Cherry-pick landed |
|---|---|---|---|---|
| 2026-06-03 | blader/Claudeception | Cherry-pick | Reimplements native skill extraction (curator + skill_manage); activator hook is a benign but token-bloating per-prompt nag; vanilla-Claude `~/.claude` paths | `references/skill-authoring-guidance.md` (retrieval-optimized descriptions + update-vs-create matrix) |
| 2026-06-03 | madaan/self-refine | Skip | Research artifact, not installable; pinned to 2023 prompt-lib/OpenAI-completion; self-critique on reasoning tasks often doesn't help without external signal | None (user opted to skip) |
| 2026-06-03 | karpathy/llm-council | Cherry-pick | Unmaintained 5-commit "vibe-coded" web app; calls OpenRouter directly (bypasses Manifest tiering); standalone UI doesn't plug into Telegram/CLI; N× cost by design | `references/council-pattern-via-delegation.md` (parallel-subagent council, anonymize-then-synthesize, cost gate) |
| 2026-06-03 | Leonxlnx/taste-skill | **Install (all 13)** | Anti-slop frontend skill pack, 32.5k stars, MIT, actively maintained (v2). Only executable `skill.sh` is a benign bash lookup table (no net/eval/writes). Real install vector is third-party `npx skills add` CLI — NOT used. SKILL.md frontmatter is Hermes-compatible, so installed natively. Off-domain (frontend, not infra) but Andrew chose full install. | N/A — installed to `~/.hermes/skills/frontend-design/` (13 skills) |
| 2026-06-03 | AgriciDaniel/claude-obsidian | **Skip** | Claude Code plugin + Obsidian vault, 6k stars, MIT, 212 commits, actively maintained. Clean security (all network localhost-only unless opt-in keys set). Core premise (hot cache + compounding wiki + hybrid retrieval) overlaps 1:1 with Hermes native memory stack (Hot tier + Honcho + LanceDB). Two skills already installed natively (`defuddle`, `obsidian-markdown`). CC hooks won't fire in Hermes. | `references/contextual-retrieval-pattern.md` (contextual-prefix chunking mapped onto LanceDB knowledge-store; 35–49% retrieval-failure reduction per Anthropic's Sept 2024 research) |\n| 2026-06-04 | CCOSTAN home-assistant-dashboard-designer (mcpmarket listing → real source `CCOSTAN/Home-AssistantConfig@master/codex_skills/`) | **Install (native)** — initial review verdict was Cherry-pick; Andrew chose full install | ~34★ single-author **Codex** skill (button-card-first dashboard DESIGN-DISCIPLINE ruleset + one Python linter). Security clean: `validate_lovelace_view.py` is stdlib + `yaml.SafeLoader` only (no net/exec/subprocess/cred reads/writes); `agents/openai.yaml` benign launcher meta; no hooks/telemetry. SKILL.md frontmatter Hermes-compatible (`---`+`name:`+`description:`) so installed natively, NOT via `npx add-skill` (Codex-only). Installed SKILL.md + references/dashboard_rules.md + scripts/validate_lovelace_view.py; SKIPPED agents/openai.yaml (Codex launcher meta) + tests/. **Env-compat caveat:** authored for Codex YAML-mode repos (`config/dashboards/**`) + button-card/card-mod/flex-horseshoe/mini-graph cards; our HA is storage-mode (`.storage/lovelace.*`) running Mushroom+Bubble+slider. Use it for its DESIGN DISCIPLINE (layout/template rules), not literal card YAML — those cards aren't installed. Validator works standalone on any view YAML. | Installed to `~/.hermes/skills/home-automation/homeassistant-dashboard-designer/`. Removal: `rm -rf` that dir. |
| 2026-06-05 | danielmiessler/Personal_AI_Infrastructure (v5.0.0) | **Cherry-pick** | 14.6K★, MIT, active dev. "Life OS" on Claude Code: 45 skills, 37 hooks, 7-phase Algorithm, ISA/ISC goal decomposition. Total overlap with Hermes skill/memory stack; 37 Bun hooks target CC event system (no Hermes equivalent); Pulse daemon expects launchd; 77% TypeScript. 3 concepts extracted. | `references/isa-goal-decomposition.md` (ISA→ISC planning template), `references/hook-driven-context-injection.md` (lifecycle-driven context injection mapped onto cron + load_when), `references/thinking-skills-as-subagent-councils.md` (7 reasoning modes via parallel subagents) |
| 2026-06-07 | nexu-io/open-design (OD, v0.9.0) | **Install — gated, plan pending greenlight** | 60.2K★, Apache-2.0, very active (2037 commits, last ~9h). Local-first "Claude Design alternative": daemon + web-UI studio app, 150 DESIGN.md systems, multi-format export (HTML/PDF/PPTX/MP4). NOT a skill pack — a standing service. Genuine non-overlap = design-system templates + PPTX/PDF/MP4 export (hand-coding can't emit those cheaply); design-to-code itself is already covered. Two integration directions both wanted: A (OD studio UI, user-driven) + B (`od mcp install hermes`, agent-driven, requires co-location). User chose **Option 2**: one install on the gateway host serving both → shared workspace. | Not yet installed. Plan: Docker route on gateway host `ubuntu-8gb-hil-1`; UI via SSH tunnel (no Tailscale on host); swapfile-first (host has no swap); `od mcp install hermes` writes MCP block (gated config edit). Rollback: `docker compose down`+`rm -rf /root/open-design`; `od mcp install hermes --uninstall`+restore config.bak. Prereqs verified: Docker 29.5.3✓, Node v22 (use Docker, OD needs 24), 5.7G RAM free, 43G disk. |

| 2026-06-11 | addyosmani/agent-skills (v1.0.0) | **Cherry-pick → Install (10 skills, native)** | 54.1k★, MIT, Addy Osmani, actively maintained, CI-validated. Hooks/commands/.gemini/agents target CC/Gemini — won't fire in Hermes, skipped. ~9/26 skills overlap existing stack (TDD, planning, debugging, simplify-code, code-review, frontend, git, brainstorming/ideation, using-agent-skills meta-skill). Initially cherry-picked 10 patterns; Andrew flipped to full native install same day. Installed via direct raw pulls (NOT upstream installer): all 12 files HTTP 200, frontmatter verified, descriptions rewritten to front-load discriminator in first 60 chars (skills_list truncation), `skills_list(category='software-engineering')` returns all 10. Also pulled orchestration-patterns.md (doubt-driven dep) + security-checklist.md. | Installed to `~/.hermes/skills/software-engineering/` (10 skills). Cherry-pick note retained: `references/addyosmani-agent-skills-cherry-pick.md`. Removal: `rm -rf ~/.hermes/skills/software-engineering/` |

| 2026-06-16 | garrytan/gstack (v1.58.1.0) | **Cherry-pick** | Garry Tan (YC president) personal Claude Code setup, MIT, actively maintained. 1,160 files (~168K TS, ~105K MD), 429 test files, 11 eval suites, 10 CI workflows — real engineering, not dotfiles. 23 "specialist" roles + 8 power tools as slash-command skills. Security clean: telemetry is LOCAL-only (`~/.gstack/analytics/*.jsonl`, gated behind consent prompt, NO amplitude/posthog/segment beacon); only outbound calls are user-configured (gbrain brain URL, local design-board reload). Footprint heavy: clones into `~/.claude/skills`, builds a browser binary, pulls Playwright+puppeteer+@huggingface/transformers+ngrok; **team mode commits a required auto-updater that silent-`git pull`s on every CC session** (supply-chain surface — pin a SHA if adopted). gbrain remote/Supabase path POSTs repo context off-machine. Vanilla-CC `~/.claude` paths + ~80-line bash preamble per skill won't slot into Hermes. README leads with an LOC vanity metric ("810× my 2013 pace") the author himself defends — judge the tools past the pitch; code was more honest than the README. | `references/gstack-methodology.md` (six forcing questions, 4 CEO scope modes, 7-specialist review army + JSON finding schema, 4-phase investigate, CSO/slop-scan/retro structure) — stored at `~/.hermes/references/`, NOT installed |

| 2026-06-16 | r/hermesagent "unblockable web stack" (Firecrawl+SearXNG+Camofox+residential proxy) | **Adopt (adapted)** | Self-hosted web stack to beat 403/Cloudflare. Adapted, not copied: residential exit was already free (Hermes runs on the home Mac mini's Comcast IP — no Pi/SSH tunnel needed); stack runs locally on the mini (hil-1 had only 3.7G RAM, not the 30G memory'd). Stood up Docker CE + SearXNG(:8888) + self-hosted Firecrawl(:3002) + Camofox(:9377), wired via `.env` (`SEARXNG_URL`/`FIRECRAWL_API_URL`/`CAMOFOX_URL`) + `web.search_backend: searxng`. | Live stack, NOT a skill. Camofox fix captured in `camofox-selfhost-fixes` skill. |
| 2026-06-16 | r/hermesagent Qwen3.6-35B-A3B variants guide | **Skip (info only)** | Local-model buyer's guide. Not actionable: no spare CUDA GPU; Intel mini is CPU-only (~40 GB/s bandwidth). Even with a 64GB Mac Studio, no open-weight model that fits beats Claude on real coding (verified DeepSWE: strongest local qwen 18%, kimi-k2.6 24%, Opus 4.8 58%). Studio = executor/orchestration/bulk/privacy/vision only; never code-gen. | `references/` n/a — folded into `model-routing-deepswe.md` |
| 2026-06-16 | r/hermesagent model-task-router (PR #43534) | **Skip / cherry-pick insight** | Auto-classify-and-dispatch skill. Wrong provider stack (routes to GPT-5.4/5.5; we run Claude+DeepSeek+local). Post's table cherry-picked (omitted Claude). **Verified live DeepSWE** flipped the conclusion: route hard coding to Opus 4.8 (58%), not Sonnet (32%); never code-gen to DeepSeek (8%) or local qwen. | `~/.hermes/references/model-routing-deepswe.md` (verified leaderboard + routing rules) |
| 2026-06-16 | r/hermesagent "Claude Code + Hermes = Massive Unlock" | **Already have it** | Run Claude Code on the Hermes host as builder/doctor via OAuth sub. We already do this (claude-code skill + claude -p + delegate_task). Top comment corroborates our orchestration-vs-coding split. Verified the "headless OAuth removed in June" comment: **announced May 14, PAUSED June 15** — usable today, but don't architect a load-bearing dependency on it. | Architectural note in `model-routing-deepswe.md` |

| 2026-06-16 | chopratejas/headroom (v0.22.3) | **Cherry-pick** (conditional install path documented) | 29.7k★, 2k forks, 1,601 commits, Apache 2.0, actively maintained (last commit today). Serious engineering: CI green, codecov, release-please, Rust crates. Compression pipeline (SmartCrusher JSON 70-90%, CodeCompressor AST-aware, CacheAligner prefix stabilization, CCR retrieval) is genuinely new — no Hermes equivalent. Memory/compaction overlap exists but the two operate at different layers (HTTP vs conversation). Native Hermes plugin in `plugins/hermes/` (PR #824 Jun 11) is clean 100-line httpx call — written by someone who knows the Hermes plugin API. 🚩 `headroom learn` auto-writes CLAUDE.md/AGENTS.md — autonomy red flag, not in the Hermes plugin, skip that CLI feature. Proxy = SPOF for all Anthropic traffic: if headroom crashes, agent loses API access. Not suitable for always-on production without supervisor. Claude Code hooks (`hooks.json`) target CC only — irrelevant for Hermes. | Two cherry-picks: (1) CacheAligner insight — keep first N chars of repeated tool-output headers stable (no per-call timestamps) for KV cache hits, no install needed; (2) CCR pattern — emit short marker + cache full content for large tool outputs, retrieve on demand (better than truncation+aux-model for web_extract timeouts). Conditional flip to install: Docker proxy on localhost + Hermes plugin only, `HEADROOM_EXCLUDE_TOOLS=read_file,headroom_retrieve`, rollback: `docker compose down` + `rm -rf ~/.hermes/plugins/headroom_retrieve/`. |

| 2026-06-16 | airweave-ai/airweave (v1.x) | **Skip** | 6.4k★, Apache 2.0, 4,775 commits, actively maintained. Unified SaaS retrieval layer: 50+ OAuth connectors (Notion/Slack/GitHub/Salesforce/etc.), continuous sync, vector index, MCP. Clean install script, solid CI (Trivy+Grype+SBOM). 🚩 AUTH_ENABLED=false by default — REST API + UI open with no auth on self-hosted. 🚩 Stack is 8+ containers: Vespa (Java ~2-4GB), Temporal workflow engine, Postgres, Redis, Svix — 6-8GB RAM minimum, massive port footprint (5432, 6379, 7233, 8081, 8001, 8080…). No Hermes-native installer. No applicable use case: value is 50-source SaaS connector ecosystem for company knowledge base retrieval — none of Andrew's projects (Mealio, dm-voice-board, Hermes core, HA) need cross-SaaS retrieval. LanceDB already covers local vector search. | Pattern cherry-pick: three-tier search model (instant=direct vector, classic=LLM picks strategy first, agentic=iterative agent reads+navigates entity hierarchy) → shapes research-heavy delegate_task subagents (search → read full entities → navigate parent/child, build comprehensive result set). |

| 2026-06-16 | Panniantong/Agent-Reach (agent-reach v1.5.0) | **Skip** | 31.9k★, MIT, Python, pushed same day, real CI + 15 test files — genuinely well-engineered (owner-only 0o600 cookie files, `shlex.quote` shell safety, rookiepy→browser_cookie3 fallback). Multi-platform read/search CLI: X, Reddit, YouTube, GitHub, Bilibili, XHS, Xueqiu, V2EX. **Quality is fine; FIT is the problem.** (1) MCP server is HOLLOW — exposes ONE tool `get_status`; its own docstring says agents should "call upstream tools directly". Installer+doctor, not a fetch integration. (2) "Zero API fees" = lifts LIVE session cookies (auth_token/ct0, SESSDATA, full cookie strings) to act AS you, or drives real Chrome via OpenCLI extension — ban-risk on an always-on home server. (3) Zero-config OpenCLI route is desktop-only; Mac mini is headless → nothing to extract, no browser to drive. (4) Heavy overlap: web/Reddit→SearXNG+Firecrawl+Camofox; X→native `x_search`; YT→`youtube-content`+yt-dlp; GH→git-trees+gh. Only non-overlap = China platforms (Bilibili/XHS/Xueqiu/V2EX), no documented use case. | None landed. Durable lessons captured as SKILL.md pitfalls instead: the "Zero API Fees" session-cookie/real-browser red-flag section + the "hollow MCP integration" check (read `@server.list_tools()` before crediting MCP support). |

| 2026-06-16 | cursor/cursor (GitHub stub → real artifact `cursor-agent` CLI) | **Skip** | The linked GitHub repo is a 5-file feedback-funnel STUB (README + SECURITY.md + issue template) — Anysphere's editor is closed-source, not here. The reviewable artifact is `cursor-agent`, the headless CLI (GA Aug 2025), a direct Claude Code competitor. Skip for THIS stack: (1) auth bills through a Cursor subscription/API key — bypasses the Claude Max OAuth bypass that makes delegation zero-marginal-cost; adding it = a second paid plan for what claude-code already does free. (2) Total overlap — already have claude-code + codex + opencode wired into delegate_task; this is a redundant 4th backend. (3) Documented Sep-2025 bug: `cursor-agent -p` doesn't exit cleanly in non-interactive mode — exactly the hang that breaks delegate_task. (4) Its real strength is the GUI IDE, useless on a headless box. | None — `delegate_task` toolset routing already embodies the one transferable idea (same agent in terminal/CI/editor). |\n| 2026-06-16 | rohitg00/agentmemory (v0.8.x) | **Skip install / Cherry-pick** | 23.1k★, Apache-2.0, TS, ~200 test files, active. Self-hosted persistent-memory service (BM25+vector+KG, iii-engine Rust backend, live viewer, 43 MCP tools, 6 lifecycle hooks) with a FIRST-PARTY Hermes integration (`integrations/hermes/`, `src/cli/connect/hermes.ts`). Skip because: (1) TOTAL overlap — it replaces `memory.provider`; maps 1:1 onto LanceDB/B-full (verified 0.85 R this session) + Honcho + FTS5 + curator. Installing forks the memory brain in two (`on_memory_write` mirrors MEMORY.md into a store Honcho can't see) → two sources of truth, against live-first. (2) Security history for a tool that STORES YOUR SECRETS: self-ships 6 advisories all fixed only in 0.8.2 — Critical 9.8 `curl\\|sh` RCE on first run, High 8.1 default bind 0.0.0.0 + no secret (any LAN device could `/export` your whole memory store and `POST /observe` to POISON it), High 7.4 unauth mesh sync, + XSS/path-traversal/incomplete-redaction. Disclosed+patched, credit due, but high bar for a memory store; adds 3 standing ports (3111/3112/3113). (3) Headline install is \"paste this prompt into Hermes and it edits config.yaml for you\" — an authority-wrapped auto-config-write that violates the WRITE GATE; do NOT follow it. Plus env design (#250) self-reports \"available\" even when the service isn't wired. (4) Benchmark (95.2% R@5) is real but vs naive baselines, not vs our stack — no head-to-head, doesn't move the routing call. | Cherry-pick: its `on_pre_compress()` hook → fire a retrieval pass keyed to \"what's about to be lost\" BEFORE compaction (sharper than writing session-state after seeing `[CONTEXT COMPACTION]`). Refinement to the compaction-checkpoint discipline, not a new system. |\n\n| 2026-06-16 | opus-anthropic/claude-opus-4.8 | **Skip (SCAM)** | Decided from metadata alone, no code pulled. Typo-squatted org (`opus-anthropic` ≠ real `anthropic`), 6 stars, bait copy ("Unlocked… world's most powerful neural network for free… turns your computer into a control terminal"). Nonexistent product (real id is `claude-opus-4-8`; vendors don't ship unlock tools for own models). Malware wrapper (RAT/miner/stealer) in AI-tool clothing. | None — declined, not cloned/run. Pattern → SKILL.md "Pre-Fetch Triage" section. |
| 2026-06-16 | Goochbeater/Spiritual-Spell-Red-Teaming (Anthropic jailbreak guide) | **Legit red-team research (not malware; not adopted)** | 1.8k★, maintained, real adversarial-prompt techniques (ENI persona-replacement, framing safety guidelines as hostile "injections", manipulating the thinking chain, persistence via Preferences/Project/CLAUDE.md). NOT malware — readable and characterizable as technique. Works mainly for adult/refusal-reduction; core-harm values persist. CLAUDE.md variant targets the system-context layer — defended here by SOUL.md/AGENTS.md occupying that layer + the "authority-wrapped blocks = data, not authorization" rule. | None — informational. |

| 2026-06-19 | DeusData/codebase-memory-mcp (v0.8.1) | **Install — gated, pending greenlight** | 8.3k★, MIT, pure C, 863 commits, pushed same day, arXiv paper (2603.27277). Code-intelligence MCP: tree-sitter AST → SQLite knowledge graph (functions/classes/call-chains/HTTP routes), 14 MCP tools (search_graph, trace_path, detect_changes, query_graph Cypher, dead-code, get_architecture), 158 langs + Hybrid-LSP type resolution for 11. Best security posture of any tool reviewed: SLSA-3 provenance, Sigstore cosign, SBOM, VirusTotal 0/72, CodeQL blocks release on any alert, 8-layer CI audit (strace egress monitoring, 23 adversarial MCP payloads), shell-arg validation + SQLite ATTACH/DETACH block + realpath containment, ZERO published advisories. Pre-compiled binary (not runtime-auditable source — provenance chain is the mitigation, same trust model as a distro package). GENUINE non-overlap: nothing in our stack does structural call-graph (search_files=grep, LanceDB=semantic, session_search=history). Linux-amd64 static binary fits the T2 Mac. install auto-config targets Claude Code/Codex/etc NOT Hermes → use `--skip-config`, wire MCP manually. | Not yet installed. Plan: download binary + verify SHA-256/SLSA → `--skip-config` → add `mcp_servers.codebase-memory-mcp` to config.yaml (gated) → copy its embedded SKILL.md into `~/.hermes/skills/` → `config set auto_index true` → gateway restart (gated, kills session) → `hermes mcp test` expects 14 tools. Retrieval-reliability detail in SKILL.md \"MCP-server retrieval reliability\" section. Removal: `rm ~/.local/bin/codebase-memory-mcp` + config block + `rm -rf ~/.cache/codebase-memory-mcp`. |\n\n## Notes\n- Council pattern (llm-council cherry-pick) depends on `delegate_task`. **Re-probed 2026-06-11: WORKING** — 1-subagent returned `status: ok` in 5s, routed to deepseek-v4-pro, no 401. Delegation-based patterns (council, doubt-driven adversarial reviewer) are usable.
