You are implementing the "Skills panel" feature for the Hermes Dispatcher Dashboard.

Repo root: /root/hermes-dispatcher/
Frontend app dir (your cwd): /root/hermes-dispatcher/app/
Backend routes dir: /root/hermes-dispatcher/routes/ (create it)
Python venv: /root/hermes-dispatcher/.venv (fastapi, uvicorn already installed; install pyyaml via /root/hermes-dispatcher/.venv/bin/pip install pyyaml)
Real skills live at: /root/.hermes/skills/  — nested as skills/<category>/<skill-name>/SKILL.md (155 SKILL.md files). Each SKILL.md has YAML frontmatter between --- delimiters with keys like name, description, and nested metadata.hermes.tags.

GOAL: Replace the hardcoded SKILLS array in the Skills panel with real filesystem data + CRUD, and make it actually work end to end (verified with curl).

=== IMPORTANT: EXISTING BACKEND STRUCTURE — FOLLOW IT ===
The canonical backend entrypoint ALREADY EXISTS at /root/hermes-dispatcher/server.py. Read it. It:
- creates the FastAPI app with CORS already configured
- defines `api_router = APIRouter(prefix="/api")` and does `app.include_router(api_router)` BEFORE a catch-all SPA fallback route
- runs uvicorn on port 8787 (the vite proxy at app/vite.config.ts already forwards /api -> http://localhost:8787)
DO NOT create main.py. DO NOT change the port. DO NOT duplicate the app. You must integrate the skills router into server.py.

=== BACKEND: create /root/hermes-dispatcher/routes/skills.py ===
Also create /root/hermes-dispatcher/routes/__init__.py (can be empty).
In skills.py define a FastAPI APIRouter named `router` (NO prefix of its own — it will be included into the existing /api router, see integration step). The skills root must be resolved from ~/.hermes/skills (os.path.expanduser). Endpoints (final mounted paths shown in parentheses):

- GET /skills            (mounted as GET /api/skills) — walk the skills root recursively. For every SKILL.md, parse its YAML frontmatter (the block between the first pair of `---` lines). Return a JSON array of:
    {id, name, description, category, tags, path}
    id = the skill directory name (dir containing SKILL.md). Build an internal index id->absolute path; on duplicate dir names, keep the dir name as id but make sure the index resolves to a real path (last one wins is acceptable, but prefer not to crash).
    name = frontmatter `name` (fallback to dir name)
    description = frontmatter `description` (fallback "")
    category = parent directory name of the skill dir (the <category> segment); for a skill directly under skills/ use "uncategorized" or its own name sensibly.
    tags = frontmatter `tags`, else metadata.hermes.tags, else []
    path = absolute path to the SKILL.md
    Parse robustly: if PyYAML fails on a file, fall back to partial/empty metadata rather than crashing the listing.

- GET /skills/{id}       (GET /api/skills/{id}) — return {id, name, category, path, content} where content is the full raw SKILL.md text. 404 if unknown.

- PUT /skills/{id}       (PUT /api/skills/{id}) — body {content: str}. Overwrite the SKILL.md. Return {ok: true}.

- POST /skills           (POST /api/skills) — body {name, category, content}. Create skills/<category>/<name>/SKILL.md (mkdir parents). Return {ok: true, id, path}. 400 if it already exists.

- DELETE /skills/{id}    (DELETE /api/skills/{id}) — shutil.rmtree the skill directory. Return {ok: true}.

Rebuild the id->path index by scanning the filesystem on each request (cheap for ~155 files) so create/delete are reflected. Guard against path traversal: resolved paths must stay within the skills root.

=== INTEGRATE INTO server.py ===
Edit /root/hermes-dispatcher/server.py to include the new router under the existing /api router. After the api_router is defined (and its /health route), add:
    from routes.skills import router as skills_router
    api_router.include_router(skills_router)
Place the include so the routes register BEFORE `app.include_router(api_router)` and BEFORE the SPA catch-all. Keep everything else intact. Final paths must be /api/skills etc.

=== VITE PROXY ===
Already configured (app/vite.config.ts proxies /api -> localhost:8787). Leave it unless broken.

=== FRONTEND: /root/hermes-dispatcher/app/src/components/panels/Skills.tsx ===
- Remove `import { SKILLS } from '../../data/skills'`. Do NOT delete data/skills.ts.
- Fetch list from `/api/skills` in a useEffect on mount; store in state. Map API fields onto the card grid (grid uses p.id, p.name, p.cat, p.desc, p.skills[] for chips, plus a UI-only toggle). Use category for `cat`, tags for the chip row (ok if empty). Default toggle `on` to true (enabled is UI-only local state).
- Keep the EXISTING card grid layout/styling intact — only swap the data source.
- On clicking a card, open the detail drawer and fetch `/api/skills/{id}` to load the full SKILL.md content. Render the raw markdown in a <pre> (monospace, pre-wrap, scrollable) in the drawer. The old drawer used detail.long/author/version/scope/calls7d/commands which no longer exist — REPLACE the drawer body with: category label, name, the loaded SKILL.md in a <pre>, and an Edit button. Keep the close button and accent styling.
- Edit button: switch drawer into edit mode with a <textarea> pre-filled with the content + Save and Cancel buttons. Save => PUT /api/skills/{id} with {content}; on success exit edit mode and show saved content. Show a small saving/saved indicator.
- Relative fetch base ('' so /api/...). Handle loading and error states minimally.
- Keep TypeScript strict-build happy (build runs `tsc -b`). Define small interfaces for API shapes. Keep the `accent` prop.

=== VERIFY (must actually do this; you are NOT root-blocked — just run commands) ===
1. /root/hermes-dispatcher/.venv/bin/pip install pyyaml  (if not present)
2. Start backend in background: cd /root/hermes-dispatcher && .venv/bin/python server.py &   (port 8787). Wait ~2s for startup. Check it's up: curl -s localhost:8787/api/health
3. curl -s localhost:8787/api/skills | python3 -c "import sys,json;d=json.load(sys.stdin);print('COUNT',len(d));print('SAMPLE',d[0])" — confirm ~155 entries each with id/name/description/category/tags/path. Print the count + one sample id.
4. curl -s localhost:8787/api/skills/<some-real-id> — confirm full SKILL.md content returned.
5. PUT test: pick a real skill, GET its content, PUT back the SAME content (no-op), confirm {ok:true} and file unchanged (do NOT corrupt real skills).
6. POST then DELETE a throwaway skill (category=_tmp_test name=_tmp_probe) to confirm create+delete; ensure the dir is gone afterward and skills/_tmp_test is cleaned up.
7. Frontend build: cd /root/hermes-dispatcher/app && npm run build — must pass with no TS errors. Fix any errors and rebuild.
8. Kill the background server.py when done.

Report exactly: api/health result, skill COUNT, a sample id, PUT result, POST/DELETE result, npm build pass/fail. If anything fails, FIX it and re-verify — do not stop on a stub.

CONSTRAINTS:
- Do NOT modify any other panel file under app/src/components/panels/ besides Skills.tsx.
- Do NOT delete or rewrite data/skills.ts.
- OK to create routes/skills.py, routes/__init__.py, and edit server.py, Skills.tsx, and (only if broken) vite.config.ts.
